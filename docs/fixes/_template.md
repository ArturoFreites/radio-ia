# Fix: [name]
> Status: TODO
> v1 | YYYY-MM-DD

## Bug
[Symptom. How to reproduce — one action if possible.]

## Root cause
`src/path/to/file.ts:LINE` — [one sentence describing what is wrong]

## Fix
<!--
What must change — the intent, not the code.
E.g. "Read from the DB table instead of calling the external API"
not "Replace listarVocesElevenLabs() with prisma.voz.findMany(...)"
-->

## Verify
[Specific action + expected result that confirms the fix works.]

## Changelog
- v1 (YYYY-MM-DD): Initial spec
